# --- dev server（vite proxy）---
# dev 期後端 FastAPI 位址。前端打相對路徑 → vite 依此代理到 FastAPI。
# 共用機 :8000 被占時，複製本檔為 .env 並改這行即可（.env 已 gitignore，不動追蹤檔）。
DEV_API_PROXY_TARGET=http://localhost:8000

# --- 瀏覽器端 client（import.meta.env）---
# 後端 base URL（doc 25：裸根 5 前綴，無 /api 無 /v1）。
# dev 期「留空」→ 走相對路徑 + 上面的 vite proxy（勿設成 http://localhost:8000，
# 否則瀏覽器會直打後端、繞過 proxy 且需要 CORS）。僅 production build / 直連時才設。
# VITE_API_BASE=

# static Bearer token（單人；開發期固定值。doc 25 §4）
VITE_API_TOKEN=dev-token
